/** LoginForm Component
 *
 * Props:
 * -login() to call in App
 *
 * State:
 * -formData
 */

 function LoginForm({ login }) {
  return (
  <div className="LoginForm">
    LoginForm
    </div>);
}

export default LoginForm;